# Primary Adapters

Concrete implementations that **call** primary ports.

These are the entry points into the application.

## Examples

- HTTP handlers and routers
- CLI command handlers
- gRPC servers
- Message queue consumers
- Test harnesses

They translate external requests into calls to primary ports.

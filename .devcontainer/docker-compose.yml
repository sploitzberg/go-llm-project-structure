version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/workspace:cached
      - ~/.config/Windsurf:/home/vscode/.config/Windsurf
    command: sleep infinity
    network_mode: bridge

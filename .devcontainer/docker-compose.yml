services:
  app:
    container_name: go-llm-project-structure
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/workspace:cached
    command: sleep infinity
    network_mode: bridge

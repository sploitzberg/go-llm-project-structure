services:
  app:
    container_name: go-llm-project-structure
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/workspace:cached
      - ~/.config/Windsurf:/home/vscode/.config/Windsurf
    command: sleep infinity
    network_mode: bridge
